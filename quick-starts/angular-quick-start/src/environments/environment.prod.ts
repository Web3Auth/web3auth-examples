export const environment = {
  production: true,
  // Replace with your production Client ID from https://dashboard.web3auth.io
  // Use Sapphire Mainnet for production — it does NOT allow localhost
  clientId: "",
};
